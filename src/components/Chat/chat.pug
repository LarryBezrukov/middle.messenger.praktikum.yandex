.chat(class=currentChatId === id ? 'chat_selected' : '')
	.chat__avatar
		if avatar
			img.chat__avatar-image(src=avatar, alt='Chat avatar')
		else
			.chat__avatar-placeholder
	.chat__content 
		div= title
		if last_message
			div Last message: #{ last_message.user.first_name }
